import 'package:flutter/material.dart';

class InstellingenPage extends StatelessWidget {
  const InstellingenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Instellingen'));
  }
}